# Private working directories (not part of the open-source project)
.claude/
.gstack/
docs/superpowers/
council/
.worktrees/

# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/
*~

# Go
*.test
*.out
coverage.out
coverage.html

# Local secret overrides — never commit
**/.env.local
**/.env.*.local
deploy/.env

# Gradle output for the local Expo modules. Regenerated by every build, and
# it was briefly committed by a careless `git add app/`.
app/modules/*/android/build/
