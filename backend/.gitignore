tmp/
bin/
build-errors.log

# Test / coverage artifacts
*.test
coverage.out
coverage.html

# Local environment overrides
.env.dev
.env.local
.env.*.local

# Local-only compose adaptation (e.g. macOS bridge networking); not for the Linux host.
docker-compose.override.yml

# Voice eval fixtures are recordings, not source (see internal/voice/testdata/README.md)
internal/voice/testdata/*.ogg
